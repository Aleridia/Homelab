[Definition]
# Stream-level keycloak abuse detector: excessive TLS sessions to keycloak SNI.
# This is coarse-grained by design because stream mode cannot inspect HTTP paths.
failregex = ^<HOST> \[.*\] sni="keycloak\..*" class="keycloak" status=\d+ .*
ignoreregex =
